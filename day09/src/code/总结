1. 能够说出使用多态的前提条件
    1.必须继承或者实现。
    2.有子类对象赋值给父类类型的引用
    3.存在方法重写


2. 理解多态的向上转型
   子类对象到父类引用
    Animal a = new Cat();  // a引用类型，存储的是地址。
    int  age = 12;       // age是基本数据类型，存储的是数据值。

3. 理解多态的向下转型
    Animal a = new Cat();
    Cat c = (Cat)a ;


4. 能够写出静态代码块的格式
    static{

    }

5. 描述final修饰的类的特点
    final修饰类: 类不能被继承。

6. 描述final修饰的方法的特点
    方法不能被子类重写。

7. 能够说出权限修饰符作用范
        权限由小到大             private         缺省       protected         public
        本类中                   √              √           √               √
        同一个包下的其他类中        X              √           √               √
        不同包下类                X              X           X               √
        不同包下的子类             X              X           √               √